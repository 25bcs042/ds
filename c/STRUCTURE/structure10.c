// create a structure to specify data on students with these attributes : roll number , name , department , course , year of joining . 
// create 2 structure varaibles . now create a function to check if two  student have same department . pass the two structure variable.
#include<stdio.h>
#include<string.h>

typedef struct student {
  int rno;
  int yoj;
  char
}